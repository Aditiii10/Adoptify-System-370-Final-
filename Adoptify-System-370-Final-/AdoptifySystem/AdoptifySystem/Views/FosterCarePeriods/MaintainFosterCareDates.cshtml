
@{
    ViewBag.Title = "MaintainFosterCareDates";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm())
{
    <h2>MaintainFosterCareDates</h2>
    <div class="form-horizontal card border-primary" style="width:60%">

        <h2 class="table-primary">Maintain Foster Care Date</h2>
        <hr />
        <input name="FosterCareDuration_Id" value="@Model.FosterCareDuration_Id" hidden />
        <div class="form-group">
            <label class="form-control-label">Duration</label>
            <div class="col-md-10">
                <input type="number" class="form-control" id="DurationTime" value="@Model.DurationTime" name="DurationTime" placeholder="DurationTime Name">
                <h1 class="invalid-feedback">Already Exists @ViewBag.err</h1>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.ActionLink("Cancel", "SearchFosterCareDates", null, new { @class = "btn btn-warning" })
                <button type="submit" name="button" value="Save" class=" btn btn-success float-right">Save</button>
            </div>
        </div>
    </div>
}