
@{
    ViewBag.Title = "ComServe1";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Communioty Service</h2>

<div class="criteria">
    <label>Date</label>
    <input type="date" id="Date" class="btn btn-outline-dark" />
    <br />
    <input type="button" id="btnTo" value="Generate" class="btn btn-primary" />
</div>
<hr />

<div class="container">
    <h2>Veterinarian Appointments</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Volunteer Name</th>
                <th>Volunteer Surname</th>
                <th>Start</th>
                <th>End</th>

            </tr>
        </thead>
        <tbody id="tAppoint">
            <tr>
            </tr>
        </tbody>
    </table>
</div>

<div id="idx1">
    <table class="table table-striped table-dark" id="tblAdded">

        <thead>
            <tr>
                <th>Volunteer Name</th>
                <th>Volunteer Surname</th>
                <th>Start</th>
                <th>End</th>
            </tr>
        </thead>
        <tbody class="Added" id="tblAdded">
        </tbody>
    </table>
</div>

@section Scripts{
    <script>
          $(document).ready(function ()
            {
            $('#idx1').hide();

            $('#btnTo').click(function () {
                var Date = $('#Date').val();
                $.ajax({
                    url: '/Reports/ComServe',
                    type: "Post",
                    dataType: "json",
                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify({ "Date": Date }),
                    success: function (data) {

                        $('#tblAdded').find("tbody").empty();
                        var i = 0;
                        var length = data.dStart.length;
                        //debugger
                        console.log(data.Dates[0]);
                        console.log(data);

                        var rowS = "<tr><td>" + data.dStart[0] + "</td><td>" + data.dEnd[i] + "</td><td>" + data.vName[i] + "</td><td>" + data.vSurname[i] +  "</td></tr>";
                        console.log(rowS);
                        $("#tAppoint").append(rowS);

                      $("idx1").show();

                    },
                    error: function (xhr, ajaxoptions, thrownError) {
                        alert(thrownError);
                    }
                });
            });
        });
    </script>

}

<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>



