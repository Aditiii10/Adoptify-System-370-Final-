@model IEnumerable<AdoptifySystem.Models.Volunteer_Work_Type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @using (Html.BeginForm("Index", "Volunteer_Work_Type", FormMethod.Get))
    {
        <b>Search by:</b>@Html.RadioButton("Searchby", "Vol_WorkType_Name", true) <text>Volunteer Work Type Name</text>
        @Html.RadioButton("Searchby", "Vol_WorkType_Description")<text>Volunteer Work Type Description</text><br />
        @Html.TextBox("search")<input type="submit" value="Search" ? />
    }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Vol_WorkType_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vol_WorkType_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Vol_WorkType_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vol_WorkType_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Vol_WorkType_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Vol_WorkType_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Vol_WorkType_ID })
        </td>
    </tr>
}

</table>
